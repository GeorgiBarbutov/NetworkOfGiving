function goToCharity(id){
    window.location.replace("http://localhost:8080/charity/" + id);
}
function goToCreator(creatorName) {
    window.location.replace("http://localhost:8080/profile/" + creatorName);
}
